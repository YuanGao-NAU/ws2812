export enum Directions {
    // LeftToRight,
    // RightToLeft,
    // TopToBottom,
    // BottomToTop,
    horizontal,
    vertical,
}

export enum StartFrom {
    LowerLeft,
    LowerRight,
    UpperLeft,
    UpperRight,
}

export enum Mode {
    S,
    Z,
}